#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    new matrix = []
    for row in matrix:
        new row = []
        for item in row:
            new row.append(item ** 2)
        new matrix.append(new row)
    return new matrix
